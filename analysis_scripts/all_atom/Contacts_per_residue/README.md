Get PDB files of each system ready to use as a reference for contacts calculations (in this case, the first post-equilibration frame of the first replica of each system). Also combine all runs of a single system into one trajectory called 'total.xtc'. Run the calculations/plotting Jupyter notebook 'coloredbyseq_calc_AAcontacts_byres_tail-DNA.ipynb' to generate the '{system}\_{entry/exit}\_tail_contacts.dat' outputs in the 'results/AA_Contacts_per_residue_raw_data/' directory. The Jupyter notebook should generate both the raw data outputs and plot them as shown in SI figures 6-9C along with their sums per N-terminal region as printed and read in 'results/AA_Contacts_per_residue_raw_data/cumulative_tail_contacts.dat'.
